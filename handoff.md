# Apex — 인수인계 문서

다른 사람(또는 다른 세션)이 이 리포를 클론해서 바로 이어서 작업할 수 있도록 정리한 문서다.
작업 규약은 `CLAUDE.md`, 기획은 `docs/PRD.md`, 단계별 제작 계획은 `docs/PRODUCTION_PLAN.md`.

---

## 1. 이 프로젝트가 무엇인가

온라인 경쟁 레이싱 게임. 매칭이 잡힌 **뒤에** 트랙이 공개되고, 제한 시간 안에 차를 세팅해서 퀄리파잉과 그랑프리를 치른 뒤 ELO가 오르내린다.

**100% 바이브 코딩으로 만든다.** 사람은 Godot 에디터를 열지 않는다. 모든 산출물은 텍스트(코드/JSON/셸)로 생성하고, 사람은 방향 제시와 플레이 피드백만 담당한다.

---

## 2. 현재 어디까지 왔는가

| 단계 | 상태 |
|---|---|
| Stage 0.1 프로젝트 골격 | 완료 |
| Stage 0.2 검증 게이트 (GUT + CI) | 완료 |
| Stage 0.3 데이터 스키마 + 검증 로더 | 완료 |
| Phase 1.1 차량 물리 코어 | 완료 |
| Phase 1.2 입력 / 카메라 / 주행 씬 | 완료 |
| Phase 1.3 트랙 스플라인 → 지오메트리 | 완료 |
| Phase 1.4 트랙 DSL 컴파일러 | 완료 |
| Phase 1.5 랩 / 섹터 / 역주행 판정 | 완료 |
| Phase 1.6 레이싱 라인 + 목표 속도 프로파일 | 완료 |
| Phase 1.7 헤드리스 AI 드라이버 | 완료 |
| PART C-4 파라미터 스윕 (밸런스 도구) | 완료 |
| **Phase 1.8 드라이빙 필 튜닝** | **다음 작업 (사람의 판정이 필요)** |

단위 테스트와 물리 프로브가 전부 통과한 상태로 올라가 있다(개수는 `./tools/check.sh`가 출력한다).

### 지금 실제로 되는 것

- **Aurora Speedway(4.0km, 14세그먼트)가 JSON에서 컴파일되어 생성된다.** `--track=<id>`로 바꿔 실을 수 있다
- 차가 그 위를 248 km/h로 한 바퀴 완주한다(측정은 오벌 기준)
- 0-100 km/h 4.1초, 최고속 350 km/h(저다운포스) / 280 km/h(고다운포스)
- 노면을 벗어나면 그립이 1.00 → 0.55로 떨어진다
- 트랙 JSON(`game/data/tracks/*.json`)이 부팅 시 검증된다 — 세그먼트 형식, 피트 인덱스 범위, 코너 각도 합 360°, 그리고 센터라인이 실제로 출발선으로 돌아오는지(2m 이내)
- 랩과 섹터가 계측된다 — 랩타임 / 섹터 스플릿 / 베스트랩, 역주행 감지, HUD 표시
- 트랙 커브에서 레이싱 라인과 구간별 목표 속도가 계산된다. 목표 속도는 다운포스가 속도에 따라 붙는 것을 반영한다(§6.21) — 저속 코너에서 차가 못 내는 속도를 요구하지 않는다
- **AI 드라이버가 레이싱 라인을 따라 혼자 완주한다.** Aurora 플라잉 랩 1:04.467 (이론값 1:01.363 대비 +5.1%), 도로 밖으로 나가지 않는다
- AI 드라이버가 슬라이드를 감지해 카운터스티어를 넣고 스로틀·브레이크를 걷는다(§6.20). 정상 코너링(최대 바디 슬립 1.7°)은 건드리지 않는다
- 랩타임 회귀 게이트가 돈다 — 플라잉 랩이 기준값에서 ±0.5초를 벗어나면 `check.sh`가 실패한다
- **파라미터 스윕이 돈다**(`./tools/sweep.sh`) — 세팅마다 성능 엔벌로프를 재고 랩타임 표를 낸다. 다섯 세팅 전부 완주하고 PRD §27이 ideal 랩 기준으로 성립한다(§6.17b, §6.22)
- 키보드/게임패드로 직접 몰 수 있고(`--autopilot`이면 AI가 몬다), 화면을 PNG로 캡처할 수 있다

### 아직 안 된 것

- 드라이빙 필 튜닝 (1.8)
- **사람이 직접 몰아본 적이 없다.** "운전이 재미있는가"라는 Phase 1 게이트는 아직 미검증이다. 이건 자동화가 대신해 줄 수 없는 유일한 항목이다

---

## 3. 클론 후 첫 5분

```bash
git clone https://github.com/ghdwnsvy0422-hub/Apex-technical-aero.git
cd Apex-technical-aero

./tools/install_godot.sh    # Godot 4.4-stable을 bin/godot에 설치 (gitignore됨)
./tools/check.sh            # 전체 검증 게이트. 여기가 초록불이면 환경 정상
```

엔진 버전을 고정하는 이유는 업데이트가 물리 거동을 조용히 바꾸기 때문이다. 그러면 원인 불명의 랩타임 회귀로 나타난다.

```bash
./tools/godot.sh --path game                              # 직접 몰아보기
./tools/godot.sh --path game -- --autopilot               # AI가 모는 것을 구경
./tools/godot.sh --headless --fixed-fps 30 --path game -- --sim --laptime  # 랩타임만
./tools/sweep.sh                                          # 세팅별 랩타임 표 (수 분)
./tools/screenshot.sh /tmp/shot.png 26                    # 시뮬 26초 시점 캡처
./tools/screenshot.sh /tmp/top.png 26 top                 # 같은 시점 하향 시점
```

조작: W/S 가속·브레이크, A/D 조향, Shift 에너지, C 카메라 전환, Tab 후방, R 리셋, Esc 종료.

---

## 4. 검증이 이 프로젝트의 중심이다

AI가 "됐습니다"라고 말하는 것과 실제로 되는 것은 다르다. `./tools/check.sh`가 순서대로 돌린다.

1. 리소스 임포트 + **GDScript 컴파일 검사**
2. 환경 자가 검증 — Jolt 활성, 120Hz, 입력 액션, 데이터 무결성
3. GUT 단위 테스트 (`game/tests/unit`)
4. 물리 프로브 (`game/tests/sim`) — 실제로 차를 몰아 확인
5. AI 드라이버 랩타임 회귀 — 레이싱 라인으로 두 바퀴를 돌고 플라잉 랩을 기준값과 비교

### 반드시 알아야 할 검증 함정 세 가지

**(a) Godot은 파스 에러를 내고도 종료 코드 0을 반환한다.** stderr에만 뱉는다. 게이트가 출력을 직접 훑어서 잡도록 해놨다. 이 장치가 없던 시절 `await` 하나를 빠뜨렸는데 게이트가 초록불을 줬다.

**(b) 수치 검증은 부호와 방향에 약하다.** 차의 앞뒤가 뒤집힌 채로 — 즉 **뒷바퀴로 조향하는 상태로** — 물리 프로브를 전부 통과한 적이 있다. 회두각의 *크기*만 쟀기 때문이다. 스크린샷 한 장이 즉시 드러냈다. 그 뒤로 프로브에 "오른쪽으로 꺾으면 오른쪽으로 간다"를 추가했다. **시각적 변경은 반드시 캡처해서 눈으로 확인한다.**

**(c) 눈으로 보는 것도 틀릴 수 있다.** "커브가 렌더링되지 않는다"고 판단할 뻔했는데, 헤드리스 레이캐스트로 단면을 찍어보니 커브는 정확히 제자리에 있었다. 640×360 상공 46m에서 1.2m 폭이 안 보였을 뿐이다.

원인 추적은 **헤드리스 진단 → 최종 확인만 캡처** 순서가 압도적으로 빠르다. 캡처 한 번에 3분 넘게 걸린다.

---

## 5. 핵심 설계 결정과 그 이유

| 결정 | 이유 |
|---|---|
| **Godot 4.4 / GDScript** | 씬(`.tscn`)과 리소스(`.tres`)가 전부 텍스트다. Unreal의 Blueprint는 바이너리라 AI가 손댈 수 없고, Unity는 GUID 참조 때문에 AI가 만든 씬이 잘 깨진다. |
| **헤드리스 전용 서버가 같은 GDScript 물리를 돌린다** | PRD §89의 서버 권위 판정이 구조적으로 공짜가 된다. |
| **`VehicleBody3D`를 쓰지 않는다** | 아케이드용이라 타이어 마모, 다운포스 트레이드오프, 부위별 손상을 넣을 수 없다. RigidBody3D + 커스텀 레이캐스트 서스펜션 + 슬립 기반 타이어 모델로 직접 구현했다. |
| **물리 틱 120Hz 고정** | 커스텀 레이캐스트 서스펜션은 60Hz에서 눈에 띄게 튄다. 서버가 클라 결과를 재현해야 해서 여유를 뒀다. |
| **트랙은 스플라인에서 절차 생성** | 트랙 추가가 JSON 파일 하나가 된다. PRD §82 라이브 콘텐츠가 사실상 공짜가 된다. |
| **리버리는 셰이더 파라미터** | PNG를 만들지 않는다. 시즌마다 수십 종 추가해도 비용이 0에 가깝다. |
| **콘텐츠는 코드가 아니라 데이터** | 모든 데이터 파일은 `schema_version`을 갖고 부팅 시 검증된다. 잘못된 콘텐츠는 크래시가 아니라 "조용히 이상한 차"로 나타나서 추적이 어렵다. |

---

## 6. 값비싸게 얻은 교훈 — 반드시 읽을 것

코드에 주석을 달지 않으므로(§7), **이 절이 그 지식의 유일한 보관처다.** 물리를 건드리기 전에 읽어라.

### 6.1 Godot의 기본 `linear_damp`가 항력을 덮어쓴다

Godot RigidBody3D의 기본 감쇠(0.1)는 속도 비례 힘이라 220 km/h에서 약 **5000 N**이 붙는다. 실제 공기저항보다 크다. 구동력 8300 N에 항력 3200 N인데 차가 가속하지 않아서, 앞바퀴 힘·차체 접지·속도 벡터까지 전부 찍어본 뒤에야 찾았다.

→ `car_body.gd`에서 `DAMP_MODE_REPLACE`로 **차단**해 놨다. 0으로 두는 것만으로는 부족하다. 월드/에어리어 기본값과 결합되기 때문이다. **절대 되돌리지 마라.**

### 6.2 엔진 관성은 기어비의 제곱으로 바퀴에 반영된다

이걸 빼먹으면 1단에서 바퀴 하나에 3000 N·m가 걸리는데 휠 관성이 0.78 kg·m²뿐이라, **한 틱(1/120초)에 바퀴 표면속도가 7 m/s씩 뛴다.** 타이어가 즉시 헛돌고 영영 못 빠져나온다.

→ `CarSetup.driven_wheel_inertia()`가 `engine_inertia * ratio²`을 더한다. 1단에서 휠 관성의 10배가 넘는다.

### 6.3 항력과 다운포스는 독립 변수가 아니다

둘을 따로 두면 윙이 **공짜 그립**이 되고, PRD §27이 무너져 모든 트랙에서 최적 세팅이 하나로 수렴한다.

→ `Aero.effective_drag_area()`가 `drag_area + induced_drag_factor * downforce_area`로 유도항력을 묶어놨다. 이 덕분에 저다운포스 350 km/h vs 고다운포스 280 km/h가 나온다.

### 6.4 기어 선택은 엔진 회전수만으로도, 도로 속도만으로도 안 된다

- 엔진 회전수 기준: 출발 시 휠스핀으로 rpm이 치솟아 **47 km/h에서 8단까지 숏시프트**한다
- 도로 속도 기준: 레브 리미터를 무시한다
- 어느 한쪽만 쓰면 두 기어 사이를 **매 틱 진동**한다

→ 둘 중 **낮은 쪽**을 쓰고, 변속 후 0.3초 쿨다운을 둔다.

### 6.5 Godot의 전방은 -Z다

앞축은 **음수 Z**에 있어야 한다. 반대로 두면 차가 뒷바퀴로 조향하고 에어로 밸런스가 뒤집히는데, **크기만 보는 테스트는 전부 통과한다.** `test_front_axle_is_ahead_of_the_rear`가 이걸 못 박아 놨다.

### 6.6 캡처는 프레임이 아니라 시뮬레이션 시간으로 기다려라

소프트웨어 래스터라이저는 느려서 Godot이 프레임당 물리 스텝 수를 제한(`max_physics_steps_per_frame`)해 버린다. 프레임 기준으로 기다리면 **출발선에 그대로 서 있는 차**를 찍는다. 로그는 280 km/h를 찍고 있는데 스크린샷은 0 km/h였다. 같은 함정에 두 번 빠졌다.

→ `screen_capture.gd`가 시뮬 시간으로 기다리고, `screenshot.sh`가 `--fixed-fps 20`으로 돈다. 해상도도 640×360으로 낮춰 놨다.

### 6.7 타이어 횡그립 정점은 슬립각 7~9도여야 한다

초기 Pacejka 계수는 정점이 **28도**에 있었다. 그러면 차가 크게 미끄러진 뒤에야 그립이 붙는 빙판 감각이 된다. 고친 뒤 같은 입력에서 회두각이 107°→161°로 살아났다. `test_lateral_grip_peaks_at_a_small_slip_angle`이 지키고 있다.

### 6.8 새 `class_name`을 추가하면 임포트를 한 번 거쳐야 한다

전역 클래스 캐시가 갱신되지 않아 "Identifier not declared"가 뜬다. `./tools/check.sh`가 임포트를 먼저 돌리므로 게이트를 통과시키면 자동으로 해결된다.

### 6.9 각도 합이 360°인 것과 트랙이 닫히는 것은 다른 문제다

`aurora_speedway.json` 초안은 코너 각도 합이 정확히 360°였지만, 세그먼트를 실제로 걸어 보면 끝점이 출발선에서 **622m** 떨어져 있었다. 각도는 방향만 말할 뿐 위치를 말하지 않는다. 직선 길이가 맞아야 루프가 닫힌다.

직선 길이는 닫힘 조건에 **선형**으로 들어가므로, 코너를 고정한 채 직선 두 개를 2×2 연립방정식으로 풀면 정확히 닫을 수 있다. 지금 배치가 그렇게 나온 것이다(오차 0.05m). 트랙을 손볼 때 코너를 바꿨다면 직선도 다시 풀어야 한다.

`TrackCompiler.closed_positions`가 남은 오차를 점들에 분산시켜 곡선을 정확히 닫지만, 그건 cm 단위 잔차를 위한 것이지 설계 오류를 덮으라고 있는 게 아니다. 그래서 검증기가 2m를 넘으면 부팅을 실패시킨다.

### 6.10 트랙 진행 방향의 부호 규약

JSON의 `angle_deg`는 **양수가 우회전**이다. `TrackLayout`이 쓰는 방위각도 같은 부호(우회전이 증가)이고, 방향 벡터는 `(sin θ, 0, -cos θ)`다(θ=0이면 Godot의 -Z 전방).

Godot의 `Vector3.rotated(Vector3.UP, a)`는 **반대 부호**로 돈다(오른손 법칙). 그래서 `TrackLayout.rotate_clockwise`가 `-angle`을 넘긴다. 이 부호를 한 번 뒤집었더니 트랙이 좌우로 뒤집힌 채 각도 합만 맞아서, 닫힘 계산이 622m가 아니라 612m로 나왔다. 숫자가 그럴듯해서 바로 눈치채기 어렵다.

### 6.11 랩 판정을 커브 오프셋으로 하면 트랙이 자기 자신을 피해야 한다

`LapTimer`는 체크포인트 콜리전 대신 `Curve3D.get_closest_offset()`이 주는 **센터라인 거리**로 진행도를 잰다. 콜리전 판정보다 단순하고, 코너를 잘라도 진행도가 정확하다.

전제가 하나 있다: 서킷의 서로 다른 구간이 가까이 지나가면 안 된다. 가까우면 코스를 벗어난 차의 최근접점이 반대편 구간으로 튀어 랩이 갑자기 오르내린다. Aurora는 서로 120m 이상 떨어진 지점들 사이의 최소 거리가 **119m**라 안전하다(런오프 포함 반폭은 23m). 새 트랙을 그릴 때 이 여유를 지켜야 하고, `test_one_lap_around_the_shipped_track`이 왕복 오차로 이걸 감시한다.

**피트레인(Phase 4.5)이 들어오면 이 전제가 깨진다.** 피트레인은 정의상 메인 스트레이트 옆을 나란히 달린다. 그때는 피트 구간을 별도 커브로 두고 진행도를 분리해야 한다.

### 6.12 결승선을 후진으로 되넘으면 가짜 베스트랩이 생긴다

진행도(누적 거리) 기반이라 랩 수는 후진하면 도로 줄어 이중 카운트가 없다. 하지만 되넘은 뒤 다시 통과하면 **0.3초짜리 랩**이 기록되어 베스트랩을 오염시켰다. 랩 수만 보고 있으면 놓치는 종류의 구멍이다.

지금은 경계를 뒤로 넘거나 역주행이 감지되면 그 랩을 `lap_valid = false`로 무효 처리하고, 무효 랩은 기록에도 시그널에도 남기지 않는다. HUD는 무효 랩의 시간을 주황색으로 표시한다.

### 6.13 GUT의 고아 노드 집계는 우리 코드만 세지 않는다

Phase 1.5 이후 실행 요약에 `Total orphans = 17`이 나타났다. 트랙 컴파일 / 레이아웃 / 랩 타이머 / 커브 샘플링 / `Track` 생성 후 `free()`까지 각 호출 전후로 `Performance.OBJECT_ORPHAN_NODE_COUNT`를 재보면 **전부 0**이다. 남는 숫자는 테스트 스크립트 인스턴스와 GUT의 시그널 감시 정리분이고, GUT 스스로도 "이 수치에 GUT 객체가 완전히 빠지지는 않는다"고 적어 둔다.

그래서 실행 요약의 숫자를 쫓지 말고, 누수를 잡고 싶은 지점에 `assert_no_new_orphans()`를 걸어라. `test_compiling_a_track_and_timing_a_lap_leaks_no_nodes`가 그 역할을 한다 — 트랙을 컴파일하고 랩을 재는 경로가 노드를 흘리면 그 테스트가 실패한다.

### 6.14 레이싱 라인은 최적화가 아니라 완화(relaxation)로 얻는다

`RacingLine`은 코너 진입/에이펙스/탈출을 규칙으로 찍지 않는다. 센터라인 샘플마다 **횡방향 자유도 하나**만 두고(도로 반폭에서 차폭 여유를 뺀 코리도), 각 점을 양옆 점의 중점 쪽으로 조금씩 당기는 라플라시안 완화를 300회 돌린다. 곡률이 최소가 되는 선이 자연히 나오고, 코리도에 clamp하는 것만으로 "도로를 벗어나지 않는다"가 보장된다.

Aurora에서 센터라인 대비 길이 4039m → 4001m, 총 곡률 −15%, 1번 코너 에이펙스는 코리도 한계(6.3m)에 정확히 물린다. 완화 횟수는 200회면 이미 에이펙스가 한계에 닿고 300회면 곡률이 0.5% 안쪽으로 수렴한다 — 그 이상은 시간 낭비다.

목표 속도는 두 단계다: 먼저 각 점의 곡률에서 `v = sqrt(a_lat / k)`, 그다음 앞뒤로 두 번씩 훑으며 가속·제동 한계로 깎는다. 이 두 번 훑기가 없으면 코너 직전에 물리적으로 불가능한 감속을 요구하는 프로파일이 나온다. 검증은 "어떤 구간도 제동 한계 2g를 넘게 요구하지 않는다"로 건다.

수치(횡 2.2g, 제동 2.0g, 가속 0.55g, 최고속 350km/h)는 물리 프로브가 실제로 측정한 값에서 가져왔다. 차량 세팅이 바뀌면 이 상수도 다시 재야 한다.

### 6.15 AI 드라이버는 순수 함수여야 하고, 랩타임은 틱에서 나와야 한다

`AiDriver`는 노드가 아니라 `RefCounted`이고, 상태를 들고 있지 않다. `input_for(transform, velocity)` 하나가 전부다. 그래야 시뮬 하네스와(나중에) 전용 서버가 같은 코드를 돌린다. 입력 스무딩은 넣지 않았다 — 그건 사람 손가락을 위한 것이라 `game/client`의 `PlayerInput`에만 있어야 한다.

세 가지가 실제로 문제였다:

**(a) 정지 상태에서 풀스로틀은 그냥 휠스핀이다.** 물리 프로브의 `_launch_throttle`이 같은 이유로 스로틀을 램프시킨다. AI도 같은 처리가 필요해서 `traction_limit()`이 저속에서 스로틀 상한을 걸어 놨다(정지 시 0.28, 16 m/s에서 해제). 없으면 출발선에서 바퀴만 태우고 랩타임이 무의미해진다.

**(b) 룩어헤드는 속도에 비례해야 한다.** 고정 거리로 조준하면 저속에서는 코너를 잘라 먹고 고속에서는 영구적으로 좌우로 흔들린다. 보정이 오차가 사라진 뒤에 도착하기 때문이다. `speed * 0.62초`를 14~75m로 클램프한다.

**(c) 랩타임은 `delta` 누적이 아니라 틱 수에서 계산한다.** `lap_probe.gd`는 `ticks / TICK_RATE`를 `LapTimer`에 넘긴다. 이래야 `--fixed-fps` 값이 달라져도 결과가 같다 — 실제로 30과 60에서 1:04.467로 완전히 일치한다. `delta`를 누적하면 §6.6의 프레임당 물리 스텝 제한이 그대로 랩타임에 새어 들어와 기준값이 머신마다 달라진다.

기준값(`BASELINE_LAP_S = 64.467`)은 **현재 차량 세팅에 묶여 있다.** 항력 계수를 0.70 → 1.10으로 올려서 게이트가 실제로 무는지 확인했다(+1.016초, FAIL). 반대로 0.74 정도의 작은 변경은 +0.116초로 통과한다 — ±0.5초는 "차가 못 달리게 됨"을 잡으라고 있는 것이지 0.1초를 잡으라고 있는 것이 아니다(계획서 PART C-3).

측정 구간은 **두 번째 랩**이다. 첫 랩은 정지 출발이라 1:09.558이고, 이걸 기준으로 삼으면 출발 로직 변경이 전부 물리 회귀로 보인다.

### 6.16 스윕은 세팅마다 목표 속도를 다시 계산해야 한다

파라미터 스윕의 첫 설계는 세팅만 바꾸고 레이싱 라인은 그대로 뒀다. 그러면 아무것도 증명되지 않는다. `RacingLine`의 목표 속도는 고정 상수(횡 2.2g, 최고속 350km/h)에서 나오므로, 다운포스를 올려도 **AI가 코너에서 더 빨리 달라고 요구하지 않는다.** 차는 빨라졌는데 운전사가 그걸 안 쓰는 상태가 된다.

그래서 `PerformanceEnvelope`가 세팅마다 최고속/횡G/제동G/가속G를 실제로 재고, `apply_to()`가 그 값으로 `compute_speeds()`를 다시 돌린다. `test_more_grip_raises_the_corner_speeds`가 이 연결을 지킨다 — 이게 끊기면 스윕은 조용히 무의미해진다.

**측정과 주행은 분리해야 한다.** `DriveHarness`는 원점에 자기 평지를 깔고, 컴파일된 트랙도 원점에 있다. 둘을 동시에 씬에 두고 측정하면 차가 배리어 안에서 스폰되어 최고속 0km/h, 횡G 3.18(→0.07) 같은 쓰레기 값이 나온다. 숫자가 그럴듯하지 않아서 다행이었지 조금만 그럴듯했으면 그대로 믿었을 것이다. 지금은 트랙을 씬에 넣기 **전에** 모든 엔벌로프를 먼저 잰다.

### 6.17 스윕이 처음 낸 결과 (2026-09, 기본 차량)

```
  downforce   top speed   lateral   ideal lap   driven lap    delta
       2.0      339 km/h    1.91 g   1:01.306     DNF       0 lap(s) only
       3.0      317 km/h    2.30 g   0:58.336   1:08.675   +7.200 s
       4.0      310 km/h    2.41 g   0:57.651   1:01.642   +0.167 s
       5.0      295 km/h    2.54 g   0:57.452   1:01.475     ---  <- quickest
       6.0      280 km/h    2.65 g   0:58.011   1:01.917   +0.442 s
```

PRD §27이 양방향으로 성립한다: 윙을 키우면 최고속이 339 → 280km/h로 떨어지고 횡G가 1.91 → 2.65로 오른다. 그리고 **최적값이 안쪽에 있다**(5.0). 6.0은 더 붙였는데 오히려 느리다. "모든 트랙에서 하나의 최적 차량"이 없어야 한다는 §27의 요구가 한 트랙 안에서만이라도 수치로 확인된 셈이다. 트랙 간 비교는 트랙이 하나뿐이라 아직 불가능하다(Phase 6.1).

**주의: driven lap 열은 밸런스 데이터가 아니다.** ideal과의 차이를 보면 4.0/5.0/6.0은 약 4초 뒤지는데 3.0은 10.3초, 2.0은 아예 완주를 못 한다. 차이는 차가 아니라 **드라이버**에서 온다. 이 표를 그대로 읽고 "저다운포스는 쓸모없다"고 결론 내리면 틀린다. 저다운포스 세팅을 정직하게 평가하려면 드라이버가 먼저 좋아져야 하고, 그게 Phase 3.9(밸런스 검증 스윕)의 선결 조건이다.

**이 표는 역사 기록이다. 현재 수치는 아래 §6.17b를 보라.** §6.18~§6.22가 이 표를 읽는 법을 뒤집어 놓았다.

### 6.17b 현재 스윕 결과 (§6.20 슬립 관리 + §6.21 속도 의존 그립 이후)

```
  downforce   top speed   lateral   ideal lap   driven lap    delta
       2.0      339 km/h    1.91 g   1:00.915   1:15.017   +16.467 s
       3.0      317 km/h    2.30 g   0:56.961   1:07.783    +9.233 s
       4.0      310 km/h    2.41 g   0:56.020   0:59.983    +1.433 s
       5.0      295 km/h    2.54 g   0:55.192   0:58.858    +0.308 s
       6.0      280 km/h    2.65 g   0:55.546   0:58.550      ---  <- quickest
```

**모든 세팅이 완주한다.** 다운포스 2.0의 DNF가 사라졌다.

**표를 읽는 법이 바뀌었다.** delta 열과 "quickest" 표시는 driven lap 기준이라 여전히 드라이버가 섞여 있다 — 그래서 6.0이 최속으로 찍힌다. PRD §27 판정은 **ideal 열**로 하고, 거기서는 최적값이 여전히 안쪽(5.0)에 있다. 이유는 §6.22.

driven과 ideal의 차이를 **드라이버 품질 지표**로 읽어라. 2.0에서 14.1초, 6.0에서 3.0초다. 이 간격이 좁아지면 드라이버가 좋아진 것이고, Phase 3.9는 이 간격이 세팅 간 밸런스 차이보다 충분히 작아야 성립한다. 지금은 아직 아니다.

### 6.18 스윕의 경계 행은 재현되지 않는다 — 세팅당 한 번씩 따로 돌려라

다운포스 3.0 행이 같은 코드에서 1:08.300으로 완주하기도 하고 DNF가 되기도 한다. 원인을 끝까지 따라가면 **측정된 최고속의 0.005 km/h 차이**다.

```
4.0을 먼저 돌린 뒤 측정:  top 317.474808 km/h  → DNF
3.0만 단독으로 측정:      top 317.480191 km/h  → 1:08.300
```

횡G·제동G·가속G는 세 값 모두 비트 단위로 동일하다. 최고속만 6번째 자리에서 갈리고, 그 차이가 `compute_speeds`를 통해 목표 속도로 번지고, 400초를 달리는 동안 증폭되어 완주와 DNF를 가른다.

물리 월드가 오염되는 것은 아니다. **완전히 동일한 세팅을 연속 두 번 돌리면 출발 상태부터 랩타임까지 비트 단위로 같다.** 정착 후 출발 상태는 세팅에만 의존한다(차를 접지면 10cm 위에서 떨어뜨리므로 낙하 중 속도가 0.5 m/s를 넘고, 그동안 다운포스가 붙어 세팅마다 정착 결과가 달라진다). 갈리는 것은 `PerformanceEnvelope._measure_top_speed`의 플래토 판정으로, 1초마다 표본을 떠서 변화량이 0.3 km/h 미만이면 멈춘다 — 앞선 실행이 남긴 미세한 부동소수점 차이가 이 판정이 멈추는 지점을 바꾼다.

두 가지 함의가 있다.

1. **경계 행(완주와 DNF 사이에 있는 세팅)의 결과로 드라이버나 물리 변경을 판정하지 마라.** 그 행은 신호가 아니라 노이즈다. 실제로 이 함정에 빠져 슬립 관리 변경이 3.0을 회귀시켰다고 잘못 판단한 적이 있다 — 격리해서 A/B를 다시 하니 오히려 0.458초 빨라진 변경이었다.
2. **드라이버 변경을 판정할 때는 세팅마다 Godot을 따로 띄워라.** 단독 실행은 재현된다(같은 조건에서 두 번 모두 317.480191 / 1:08.300).

전체 표는 여전히 "어떤 세팅이 왜 빠른가"를 읽는 용도로 유효하다. 다만 DNF 근처의 행은 측정이 아니라 동전 던지기다.

### 6.19 저다운포스 DNF의 원인은 슬립 관리가 아니라 속도에 무관한 목표 속도다

`RacingLine.compute_speeds`는 코너 속도를 `v = sqrt(lateral_g * g / k)`로 잡는데, 여기서 `lateral_g`는 **상수 하나**다. `PerformanceEnvelope`는 그 값을 180 km/h 스키드패드에서 잰다.

문제는 횡그립이 속도에 비례해 커진다는 것이다. 다운포스는 `v²`에 비례하므로 저다운포스 차일수록 저속 코너와 고속 코너의 그립 차이가 크다. 180 km/h에서 잰 1.91g를 135 km/h 코너에도 그대로 요구하면, 그 코너에서는 차가 실제로 낼 수 없는 속도를 목표로 삼게 된다. 카운터스티어로 고칠 수 있는 종류의 문제가 아니다 — 낼 수 없는 그립을 요구받는 차는 무엇을 해도 코스를 벗어난다.

슬립 관리(§6.20)를 넣고도 다운포스 2.0이 여전히 DNF인 이유가 이것이다. 실제로 2.0 차를 추적해 보면 스핀 자체는 막히지만 코너 진입에서 이미 라인 밖으로 6.5m 밀려 있고, 결국 배리어(중심선에서 22.7m)에 박혀 남은 300초 동안 빠져나오지 못한다.

해법은 §6.21에 있다. **이 절이 처음 제안했던 "두 속도에서 재서 선형으로 분해한다"는 방법은 실제로 해보니 틀렸다** — 왜 틀렸는지가 §6.21의 절반이다.

### 6.20 드라이버의 슬립 관리는 정상 코너링을 건드리면 안 된다

`AiDriver`는 바디 슬립각(진행 방향과 차체 전방 사이의 각)을 재서 세 가지를 한다: 슬라이드 쪽으로 카운터스티어, 스로틀 상한, 브레이크 상한. 셋 다 `input_for`에 넘어오는 값만으로 계산되므로 §6.15의 "상태 없는 순수 함수" 규약은 그대로다.

데드밴드(`SLIP_DEADBAND_RAD = 0.10`, 약 5.7°)가 이 기능의 핵심이다. **기본 차량이 게이트 랩에서 내는 최대 바디 슬립은 1.7°다.** 데드밴드를 그 위에 두었기 때문에 통과 중이던 케이스는 전혀 건드리지 않고 — 랩타임이 1:04.467로 비트 단위까지 동일하다 — 진짜 슬라이드에서만 개입한다. 데드밴드를 1.7° 아래로 내리면 드라이버가 정상 코너링과 싸우기 시작해 오히려 언더스티어로 느려진다.

수치의 출처는 추적 로그다. 다운포스 2.0 차는 스핀 직전에 **슬립 41.7도에서 스로틀 89%**를 넣고 있었다. 후륜구동이 그 상태에서 회복할 방법은 없다. 그래서 스로틀 상한이 데드밴드를 넘는 순간부터 급격히 떨어지도록(`SLIDE_THROTTLE_SPAN_RAD = 0.16`, 바닥 0.10) 잡혀 있다. 브레이크도 같은 이유로 걷는다 — 미끄러지는 중의 제동은 마찰원을 종방향으로 써버려 횡그립을 더 깎는다.

효과는 작다. 격리 측정으로 다운포스 3.0이 1:08.758 → 1:08.300(−0.458초), 기본 차량은 변화 없음, 2.0은 여전히 DNF다. **저다운포스 DNF는 이걸로 해결되지 않는다** — 원인이 §6.19에 있기 때문이다.

### 6.21 그립 곡선은 피팅하지 말고 이미 아는 물리에서 세워라

§6.19가 요구한 "속도에 따라 변하는 목표 속도"를 두 가지 방법으로 시도했다. 첫 번째는 실패했고, 그 실패가 두 번째를 설계해 주었다.

**틀린 방법 — 스키드패드 2점 피팅.** 110 km/h와 180 km/h로 진입해 각각 횡G를 재고, 두 점을 잇는 직선으로 `a_lat(v) = a0 + b*v^2`를 분해하려 했다. 나온 값이 물리적으로 말이 안 됐다.

```
df 2.0  mechanical 1.50 g   aero 0.00186 1/m
df 3.0  mechanical 1.15 g   aero 0.00508 1/m   <- 4.0보다 aero가 크다
df 4.0  mechanical 1.28 g   aero 0.00483 1/m
```

타이어와 질량이 동일하므로 mechanical(에어로를 뺀 성분)은 세팅과 무관하게 거의 같아야 하고 aero는 윙 면적에 단조증가해야 한다. 둘 다 어긋난다. 원인은 표본 자체다.

```
df 3.0: 1.830 g @ 36.1 m/s  ->  2.299 g @ 47.0 m/s   (+0.469)
df 4.0: 1.946 g @ 36.7 m/s  ->  2.409 g @ 47.8 m/s   (+0.463)
```

윙이 더 큰 4.0이 속도를 올려도 3.0보다 덜 얻는다. **스키드패드 반경이 창발적이기 때문이다.** 풀락을 주고 도는 반경은 고정된 값이 아니고, `CarBody._update_steering`이 `steer_speed_reference`(75 m/s)를 기준으로 고속에서 조향 권한을 줄이므로 빠른 표본은 더 큰 반경을 돈다. 즉 두 표본은 같은 코너를 재고 있지 않다. 여기서 나온 기울기는 그립의 속도 의존성이 아니라 조향 권한 곡선과 섞인 값이다.

교훈은 일반적이다. **창발적인 양을 두 점에서 재서 선형 분해하지 마라.** 두 점이 같은 조건이라는 보장이 없으면 기울기는 원하는 물리가 아닌 다른 것을 담는다.

**맞는 방법 — 아는 것은 계산하고 모르는 것만 잰다.** 다운포스 곡선은 이미 `Aero`에 있고(`0.5*rho*Cl*A*v^2`), 하중에 대한 그립 반응도 이미 `TyreModel.grip_limit`에 있다(`load^sensitivity`, 기본 0.85). 모르는 것은 "이 차가 실제로 내는 절대 그립" 하나뿐이고, 그건 스키드패드 한 번으로 충분하다.

```
load(v) / load(0) = 1 + r*v^2,   r = Aero.load_ratio_per_speed_squared(setup, g)
a_lat(v) = a_measured * ((1 + r*v^2) / (1 + r*v_measured^2))^sensitivity
```

`r`은 다운포스를 정지 시 무게로 나눈 값이라 `CarSetup`에서 바로 나온다. 지수는 실제로 시뮬레이션되는 타이어 모델과 **같은 값**을 쓴다 — 이게 핵심이다. 목표 속도가 물리와 다른 모델을 쓰면 드라이버는 영영 못 맞춘다.

결과가 물리적으로 일관된다.

```
df    120 km/h   180 km/h(측정)   280 km/h
2.0     1.71 g       1.91 g        2.63 g
3.0     1.96 g       2.30 g        3.41 g
4.0     1.96 g       2.41 g        3.76 g
5.0     2.02 g       2.54 g        4.17 g
6.0     2.09 g       2.65 g        4.59 g
```

모든 속도에서 윙 면적에 단조증가하고, 모든 세팅에서 속도에 단조증가한다.

**코너 속도는 닫힌 형태로 안 풀린다.** 지수 0.85 때문에 `v^2*k = a_lat(v)`가 초월방정식이 된다. 고정점 반복 `v <- sqrt(a_lat(v)*g/k)`로 푼다. 수렴 인자는 `sensitivity * r*v^2/(1+r*v^2)`이라 최악 0.85로 항상 수렴하지만 **느리다.** 처음에 8회로 잡았다가 `test_the_corner_speed_solve_settles`(답이 실제로 그 반경에서 유지 가능한 속도인지 검사)에 걸렸다. 지금은 60회다. 라인 생성 시 한 번만 도는 계산이라 비용은 무시할 만하다.

이 반복은 라인 구축 시점에만 돈다. 드라이버는 여전히 완성된 속도 프로파일을 조회할 뿐이므로 §6.15의 순수 함수 규약은 그대로다.

**기본값은 옛 동작을 정확히 재현한다.** `aero_load_ratio = 0`이면 `grip_at_speed`가 측정값을 그대로 돌려주고 `corner_speed`는 `sqrt(a*g/k)`가 된다. 그래서 기본 레이싱 라인을 쓰는 랩타임 게이트가 1:04.467로 비트 단위까지 그대로다. `test_the_default_speed_profile_is_unchanged_by_the_new_parameters`가 이걸 샘플 단위로 지킨다.

### 6.22 밸런스 체크는 driven lap이 아니라 ideal lap으로 해야 한다

§6.21을 넣고 나서 스윕의 `_check_no_universal_setup`이 실패했다. 최속 세팅이 6.0(끝값)으로 나왔기 때문이다. 그런데 ideal 열은 5.0을 최속으로 가리키고 있었다.

```
downforce   ideal lap   driven lap   드라이버 페널티
     2.0    1:00.915    1:15.017        +14.1 s
     3.0    0:56.961    1:07.783        +10.8 s
     4.0    0:56.020    0:59.983         +4.0 s
     5.0    0:55.192    0:58.858         +3.7 s
     6.0    0:55.546    0:58.550         +3.0 s
```

드라이버 페널티가 다운포스에 **단조 감소**한다. 그립이 많은 차일수록 추종 제어가 관대해지기 때문이다. 그래서 driven 열로 순위를 매기면 항상 최고 다운포스가 이긴다 — 차가 아니라 드라이버를 재고 있는 것이다. §6.17이 "driven lap 열은 밸런스 데이터가 아니다"라고 적어 놓고서 정작 체크는 그 열을 읽고 있었다.

지금은 PRD §27 판정을 `ideal_s`로 한다. ideal은 측정된 엔벌로프에서 나오므로 차의 실제 성능이고, 드라이버 실력이 섞이지 않는다. 게다가 완주 여부와 무관하게 모든 행에 값이 있어서 DNF가 있어도 밸런스 판정이 가능하다.

driven 열은 남아 있고 읽을 가치가 있다. 다만 그건 **드라이버 품질 지표**로 읽어라 — ideal과의 차이가 줄어들면 드라이버가 좋아진 것이다. 별도로 `every measured setup completes a lap` 체크를 추가했다. 이건 드라이버 회귀를 잡는 용도이고, §6.21 이전이었다면 다운포스 2.0에서 실패했을 것이다.

---

## 7. 코딩 규칙

`CLAUDE.md`가 정본이다. 여기서는 가장 중요한 것만 옮긴다.

### 주석을 달지 않는다

**코딩 할 때는 어떠한 경우에도 주석을 달지 않는다.**

코드가 무엇을 하는지는 이름으로 드러낸다. 설명이 필요하면 함수를 쪼개고 이름을 고친다. 배경 지식과 의사결정 근거는 코드가 아니라 이 문서(§6)와 `docs/`에 남긴다.

### 그 밖

- **정적 타입 필수.** 타입 없는 선언은 경고로 잡힌다. 예외 없음
- 파일/함수/변수 `snake_case`, 클래스 `PascalCase`, 내부 전용 `_밑줄`
- 식별자는 영어로, 문서는 한국어로
- `game/core`는 **헤드리스에서 돌아야 한다.** `Input.*`, `get_viewport()`, 카메라 참조 금지. 입력은 `DriverInput` 값으로 주입한다
- 물리 상태 변경은 `_physics_process` 안에서만
- `print()` 대신 `Log` 오토로드
- 스탯/슬롯/등급 문자열은 `CarStats`가 단일 출처
- 모든 작업은 `main`에 커밋/푸시. 커밋 제목은 영어 명령형, 본문은 **왜**

---

## 8. 디렉터리

```
game/core     결정론적 시뮬레이션 코어 (물리, 차량, 타이어)
game/ai       AI 드라이버 (라인 추종 → DriverInput)
game/client   클라이언트 전용 표현 계층 (입력, 카메라, 비주얼, 캡처)
game/track    스플라인 → 절차적 트랙 메시 생성
game/data     부품 / 트랙 / 티어 정의 (JSON)
game/ui       Control 노드
game/tests    GUT 단위 테스트 + 헤드리스 시뮬 프로브
game/net      (비어 있음) 복제, 클라 예측, 서버 재조정
game/server   (비어 있음) 헤드리스 진입점, 룸 관리
tools/        Godot 설치, 검증 게이트, 스크린샷
backend/      (비어 있음) Go 서비스
docs/         PRD, 제작 계획, 애셋 출처
```

---

## 9. 알려진 미해결 사항

| 항목 | 내용 |
|---|---|
| **조종감 미검증** | 사람이 직접 몰아본 적이 없다. Phase 1 게이트("운전이 재미있는가")가 열려 있다 |
| 정지 풀스로틀 휠스핀 | 1000마력 후륜구동의 실제 거동이고 PRD §30에 필요한 성질이지만, 플레이어 체감에서 가혹한지는 미판정. Phase 1.8에서 결정 |
| 차체는 프리미티브 | 박스 조합이다. 계획서 PART B-2의 3단계 교체 예정 |
| 캡처 해상도 640×360 | 소프트웨어 렌더링 제약. GPU가 있는 환경에서는 올려도 된다 |
| 랩타임 기준값은 차량 세팅에 묶임 | 차량/물리를 의도적으로 바꾸면 `BASELINE_LAP_S`도 갱신하고 이유를 커밋 본문에 남겨야 한다. 설명 없이 옮기면 게이트를 끈 것과 같다 |
| AI가 사람보다 느리다 | 이론 랩 대비 +5.1%다. 단순 추종 제어의 한계이고, 회귀 감지에는 충분하다. 밸런스 스윕(3.9)에 쓰려면 더 정확해져야 한다 |
| **드라이버가 이론 랩을 못 따라간다** | ideal 대비 다운포스 2.0에서 14.1초, 6.0에서 3.0초 뒤진다(§6.17b). 이 간격이 세팅 간 밸런스 차이보다 커서 **Phase 3.9의 driven 열은 아직 밸런스 데이터가 아니다.** 다음에 좁혀야 할 것이 이것이다 |
| 스윕 경계 행은 재현되지 않는다 | 최고속 측정 0.005 km/h 차이가 완주와 DNF를 가른다(§6.18). 드라이버 변경을 판정할 때는 세팅마다 Godot을 따로 띄워라. 지금은 모든 행이 여유 있게 완주해서 덜 물리지만 원인은 남아 있다 |
| 스핀하면 영구히 못 돌아온다 | 배리어(중심선 22.7m)에 박히면 후진 기어가 없어 남은 시간을 그대로 버린다. 한 번의 실수가 곧 DNF다 |
| 브레이크·가속 한계는 여전히 상수다 | 횡그립은 속도 의존이 됐지만(§6.21) `braking_g`와 `acceleration_g`는 아직 단일 값이다. 제동도 다운포스를 받으므로 같은 처리가 필요하다 |
| 그립 모델은 노면을 모른다 | `grip_at_speed`는 노면 마찰 1.0을 가정한다. 커브나 런오프를 밟는 라인은 실제보다 낙관적으로 계산된다 |
| 스윕은 트랙 하나만 본다 | §27이 요구하는 "트랙마다 최적 세팅이 다르다"는 트랙이 늘어나야(6.1) 검증 가능하다 |
| `CurveBuilder.from_points`는 닫힌 루프 전용 | 직선을 넣으면 시작점에서 되접힌다. 개방 트랙이 필요하면 확장해야 한다 |
| GUT 12MB 벤더링 | 9MB가 에디터 GUI다. 제거를 시도했더니 임포트가 멈춰서 되돌렸다 |

---

## 10. 다음 작업 — Phase 1.8 드라이빙 필 튜닝 (그리고 Phase 1 게이트)

**여기서부터는 자동화가 대신해 줄 수 없다.** 계획서 PART F는 이 게이트에서 NO가 나오면 1.1로 돌아가고 "여기서 타협하지 않는다"고 못 박았다. 지금까지 만든 것은 전부 "차가 물리적으로 말이 되는가"를 검증할 뿐, "재미있는가"는 한 번도 물어본 적이 없다.

```bash
./tools/install_godot.sh
./tools/check.sh
./tools/godot.sh --path game
```

20분 이상 그냥 달려 보고 판정한다. 특히 봐야 할 것:

1. **정지 풀스로틀 휠스핀** — 1000마력 후륜구동의 실제 거동이고 PRD §30(운전 스타일과 마모)에 필요한 성질이지만, 플레이어 체감에서 가혹한지는 미판정이다. AI는 `traction_limit()`으로 피해 가지만 사람에게 같은 보조를 줄지는 별개의 결정이다(PRD §58 주행 보조)
2. **조향 응답** — 슬립각 정점을 7~9도로 옮긴 뒤(§6.7) 회두가 살아났지만, 그게 "무겁다/가볍다" 중 어느 쪽으로 느껴지는지
3. **제동** — 2.1g가 나오는데 이게 체감상 충분히 극적인지
4. **다운포스 트레이드오프가 손에 잡히는가** — 350 vs 280 km/h는 숫자로는 크지만 운전 중에 느껴져야 PRD §27이 의미를 갖는다

NO가 나오면 무엇이 이상한지만 적어 두면 된다. 그 피드백이 1.8의 입력이다. 파라미터 스윕으로 후보를 뽑고 랩타임 회귀로 안전망을 치는 구조는 이미 서 있다.

YES가 나오면 **Phase 2(멀티플레이)**로 간다. `game/net`과 `game/server`가 아직 비어 있고, 계획서 PART G가 지목한 최대 리스크는 2.3의 네트워크 충돌 보정이다.
